%tr
  %th Album
  %th # of Pictures
  %th Preview
-@albums.each do |album|
  %tr
    %td= link_to(album.name, album_path(album.id))
    %td= album.photos.length
    %td
      .preview= album.photos[0]
      .preview= album.photos[1]
      .preview= album.photos[2]