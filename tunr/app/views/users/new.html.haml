/ %div.happy this is happy text
/ %div#sad this is sad text

/ .super this is super
/ #bad this is bad

/ %input{:type => "text", :autofocus => true}
/ %a{:href => "http://janezila.tumblr.com/"} Jane's Tumblr

/ %h1 Hello
/ %p this is a p tag
/ %span= 3**9
/ %div
/   this is h1 content...
/   %h2 this is h2
/   %h3 this is h3
/   %p
/     %span span 1
/     %span span 2

/ - numbers = (0..10).to_a
/ - numbers.each do |num|
/   %p= num

.row
  .five.columns
    =render(:partial => 'shared/errors', :locals => {:object => @user})

    =form_for @user do |f|
      =f.label :name
      =f.text_field :name, :autofocus => true
      =f.label :image
      =f.text_field :image
      =f.label :balance
      =f.text_field :balance
      =f.label :password
      =f.password_field :password
      =f.label :password_confrimation
      =f.password_field :password_confirmation
      =f.submit nil, :class=>"button radius"
  .seven.columns