there are <%= pluralize(@personcount1, "goose")%><br>

there are <%= pluralize(@personcount2, "goose")%><br>

<%= truncate(@story, :length =>20) %>


<ul>
  <% @numbers.each do |num| %>
  <li style="background-color: <%= cycle("red", "green", "orange", "purple") %>;"><%= num %></li>
  <%end%>
</ul>
